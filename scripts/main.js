(function($, Drupal) {

  'use strict';

  /**
  * An example segment of code to execute
  */
  Drupal.behaviors.example = {
    attach: function (context, settings) {
      // Code
    }
  };

})(jQuery, Drupal);
