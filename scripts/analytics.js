var _gaq = _gaq || [];
(function($){

  /**
  * An example segment of code to execute
  */
  Drupal.behaviors.exampleAnalytics = {
    attach: function(context, settings) {
      // Code
    }
  };

})(jQuery);